apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: ingressapp
  labels:
    app: ingress
spec:
  replicas: 5
  selector: 
    matchLabels:
      app: ingress
  template:
    metadata:
      labels:
        app: ingress
    spec:
      containers:
      - name: ingress-demo
        image: red/server:1.0
  strategy:
    canary:
      stableService: ingressapp-stable
      canaryService: ingressapp-canary
      steps:
      - setWeight: 20
      - pause: { duration: 30}
      - setWeight: 50
      - pause: { duration: 30}
